/* ============================================================================
   VOICE CONFIG (optional) — ElevenLabs TTS for announcements
   ----------------------------------------------------------------------------
   This local copy is a PLACEHOLDER and is safe to commit. At deploy time the
   GitHub Action overwrites it with the real key from the EL_KEY repo secret.

   NEVER paste a real API key into this file and commit it.
   ========================================================================== */

const VOICE_CONFIG = {
  enabled: false,
  apiKey: "",
  voiceId: "21m00Tcm4TlvDq8ikWAM"
};
